# 班級積分堂 開發規劃表

需求排序全部來自研習問卷 22 筆回覆的實際票數，不是憑感覺排的。
每一項都寫了驗收條件——沒達到就不算做完。

## 現況

| 項目 | 狀態 |
|---|---|
| 第一期（記錄／稽核／儀表板） | 已完成 |
| 第二期（名冊／行為卡／學生端／商店／家長端） | 已完成 |
| 第三期（學期評語／親師草稿／導師週報／學生代碼） | 已完成 |
| 儲存層 | localStorage，介面已收斂成 append／query／void |
| 測試 | `npm test`，210 項 |
| 部署 | CF Pages + Netlify |
| 真實班級可用 | 名冊已可自建，但仍無登入與跨裝置同步（見 B.1–B.3）|

---

## 第一期：課堂當下的掌控 — 已完成

對應問卷需求第 1、2、3、5 名，合計 50 票。

| # | 任務 | 驗收條件 | 狀態 |
|---|---|---|---|
| 1.1 | 事件流資料模型 | 分數為投影而非儲存值；撤銷不刪資料 | 完成 |
| 1.2 | 座位表一鍵記錄 | 點學生到完成記錄 ≤3 秒、≤2 次點擊 | 完成 |
| 1.3 | 多選批次記錄 | 選 3 人記一次，3 人同時變動、可一次撤銷 | 完成 |
| 1.4 | 補登與稽核 | 可篩選、可撤銷、撤銷後仍查得到 | 完成 |
| 1.5 | 告急規則引擎 | 兩條規則在示範資料上穩定觸發 | 完成 |
| 1.6 | 儀表板 | 正向佔比、逐日趨勢、學生時間軸 | 完成 |
| 1.7 | 手機版面 | 400px 寬六欄座位表，姓名與分數可讀 | 完成 |

---

## 第二期：讓學生和家長看得到 — 已完成

問卷第 6 名親師群組整合 8 票、數據儀表板 7 票。這一期的重點是把老師記的東西
變成學生的動機與家長的信任，否則老師記三週就會停。

| # | 任務 | 說明 | 驗收條件 | 狀態 |
|---|---|---|---|---|
| 2.1 | 學生端排行榜 | 唯讀頁，可切換「個人成長」與「班級榜」 | 不顯示負向明細，只顯示淨分與進步幅度 | 完成 |
| 2.2 | 積分商店 | 老師自訂獎勵與所需點數，學生申請、老師核可 | 兌換會產生一筆負向事件，稽核頁查得到 | 完成 |
| 2.3 | 家長端成長頁 | 一位學生一個唯讀連結 | 頁面上不得出現任何 kind 為 improve 的資料，需寫測試守住 | 完成 |
| 2.4 | 行為卡自訂 | 老師增刪改行為卡與分數 | 改動不影響既有事件的歷史分數 | 完成 |
| 2.5 | 班級與名冊管理 | 新增班級、匯入座號姓名、拖曳排座位 | 可貼上 Excel 兩欄直接匯入 | 完成 |

**2.3 的紅線**：家長端不是「把負向藏起來」，是資料層根本不送出去。
`rules.js` 的 `parentView()` 已經先做好這件事，第二期只是接上介面。

---

## 第三期：省下老師最多時間的一刀 — 已完成

問卷 AI 輔助生成文字只有 5 票，但那是因為老師想像不到能做什麼。
一學期累積的行為事件正是寫評語最缺的素材，這一刀省下的時間最多。

| # | 任務 | 說明 | 驗收條件 | 預估 |
|---|---|---|---|---|
| 3.1 | 學期評語生成 | 把某生整學期事件摘要成評語 | 評語內容必須可追溯到具體事件，不得虛構 | 完成 |
| 3.2 | 親師溝通草稿 | 針對特定事件生成聯絡簿或電話溝通要點 | 語氣正向、具體、不下結論式標籤 | 完成 |
| 3.3 | 導師手札週報 | 每週自動整理班級狀況與需關心名單 | 名單依告急規則產生，非 AI 自由發揮 | 完成 |
| 3.0 | 學生端個人代碼 | 取代「下拉選單選誰就看得到誰」 | 改網址或改 sessionStorage 都繞不過去 | 完成 |

**「不得虛構」是怎麼保證的：** 三個功能都先由資料組出完整骨架，AI 只做語氣潤飾。
`functions/api/ai` 拿骨架與事實包的數字當白名單，潤完的文字只要冒出白名單外的
阿拉伯數字就退回骨架。`AI_ENABLED` 關著時三個功能照樣完整可用。

---

## 跨期基礎建設

| # | 任務 | 觸發時機 | 說明 |
|---|---|---|---|
| B.1 | Turso 儲存層 | 要拿去真實班級用之前 | 實作與 `store.js` 同介面的 `TursoStore`，上層 UI 不改 |
| B.2 | Google 登入 | 同上 | 沿用文豪笑傳那套；一位老師一份資料 |
| B.3 | 個資盤點 | 同上，且必須早於 B.1 上線 | 學生姓名進雲端前要先確認學校端規範與保存期限 |
| B.4 | 匯入／匯出 | 第三期前 | 名冊可貼上匯入了，但整份資料仍只能匯出 JSON、無法匯回；換裝置與備份都需要 |
| B.5 | 單元測試 | 進行中 | `parentView` 已有 7 項測試；`rules.js` 其餘純函式與 `shop-core.js` 尚未補齊 |

**B.3 不是行政流程，是這個專案能不能真的用的前提。**
第一期資料只在老師自己的瀏覽器，沒有這個問題；一旦上雲端就有。

---

## 建議的下一步

功能面該有的都有了，卡住的是**開關還全部關著**：`CLOUD_ENABLED` 與 `AI_ENABLED`
都是 false，雲端與潤稿的程式碼寫完也測完，但沒有真的跑起來過。

要真的帶進班級用一學期，剩下的是三件不是寫程式的事：

1. **個資盤點的學校端三個問題**（`docs/privacy-review.md`）——這份資料算不算校務系統
   的範圍、要不要家長同意書、學校對保存年限有沒有另外規定。這三題沒答案，
   `CLOUD_ENABLED` 就不該打開。
2. **`privacy.html` 是掛老師名字的政策聲明**，要老師自己讀過才能發布。
3. **申請 Google OAuth 與 Turso**，把環境變數設好，才輪得到打開開關。

程式面剩下的兩件（都不急）：
- `detectAlerts()` 的時間錨點寫死在「今天」，回顧過去某一週要靠撐大觀察窗繞過去。
  改成接受 `now` 參數（預設 `new Date()`）就乾淨了，週報也能一併簡化。
- 學生端與家長端在 `CLOUD_ENABLED` 打開後的實機驗證——目前只有測試與本機模式驗過。
